name: skilllock

on:
  pull_request:
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: lz1834career/skilllock/action@v1.0.0
        with:
          command: check
          skip-tests: "true"