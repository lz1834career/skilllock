# Example only — copy to skilllock-check.yml to enable on pull_request/push.
name: skilllock-check

on:
  workflow_dispatch:
  # pull_request:
  # push:
  #   branches: [main]

jobs:
  skilllock:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./action
        with:
          command: check
          skip-tests: "false"
